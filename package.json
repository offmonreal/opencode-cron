{
  "name": "opencode-cron",
  "version": "1.0.0",
  "description": "CronCreate MCP tool for OpenCode — schedules recurring prompts into an active session",
  "type": "module",
  "bin": {
    "opencode-cron": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
